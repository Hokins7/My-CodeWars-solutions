/**
 * This JavaScript code defines a function 'maps' that takes an array of integers and returns a new array with each value doubled.
 *
 * @param {number[]} x - The input array of integers.
 * @returns {number[]} - A new array where each value is doubled.
 */
function maps(x) {
    return x.map(v => v * 2);
  }
  