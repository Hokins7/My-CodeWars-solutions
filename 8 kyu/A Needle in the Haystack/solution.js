/**
 * This JavaScript code defines a function 'findNeedle' that searches for the word "needle" in an array and returns a message.
 *
 * @param {string[]} haystack - The input array containing the words.
 * @returns {string} - A message indicating the position where "needle" was found.
 */
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
  }
  