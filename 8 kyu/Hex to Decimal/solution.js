/**
 * Convert a hex number (given as a string) to a decimal number.
 * @param {string} hexString - The hexadecimal number as a string.
 * @returns {number} - The decimal representation of the hex number.
 */
function hexToDec(hexString) {
    return parseInt(hexString, 16);
  }
  