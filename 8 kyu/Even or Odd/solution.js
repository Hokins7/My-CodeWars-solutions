/**
 * Determine if a number is even or odd.
 * @param {number} number - The number to be checked.
 * @returns {string} - 'Even' if the number is even, 'Odd' if it's odd.
 */
function even_or_odd(number) {
    return number % 2 === 0 ? 'Even' : 'Odd';
  }
  